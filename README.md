# ChatFlow

A simple web dashboard for viewing active conversations and responding to messages.

## Features

* Lists all open sessions with last message timestamp
* Clicking a session opens the full message history
* Agent can send messages from the dashboard and they appear instantly in the customer view

## Usage

1. Run `python -m pytest` to run the tests
2. Run `python -m chatflow` to start the chatflow server
